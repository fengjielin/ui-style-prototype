# 童蹊社区-原型 — 积分方案维护设计

> 文档类型：设计 | 日期：2026-08-12 | 版本：v1.0
> 关联需求：`2026-08-11-01-需求-童蹊社区-原型需求拆解.md` | 关联方案：`2026-08-11-02-方案-童蹊系统菜单模块设计.md`
> 落地范围：`ui-style-prototype/medal-system/`（纯静态原型）

---

## 目录

1. [背景与目标](#1-背景与目标)
2. [关键决策](#2-关键决策)
3. [数据模型改造](#3-数据模型改造)
4. [页面布局](#4-页面布局)
5. [交互细节](#5-交互细节)
6. [校验与边界](#6-校验与边界)
7. [改动文件清单](#7-改动文件清单)
8. [非本次范围](#8-非本次范围)

---

## 1. 背景与目标

需求文档 3.1.2 / 3.2 提出的「积分规则配置管理模块」，当前原型的实现存在三处偏差：

1. 月度常规与专项活动方案**平铺在同一列表**，共用一套「统一权重配置面板」；
2. **所有方案（含月度常规）均可「复制方案」**；
3. 活动方案未独立承载「活动周期 / 参与对象 / 奖励折算标准」。

本次设计目标：

- **月度常规勋章积分方案**：固定唯一一套、不可复制、始终生效；
- **活动方案**：可维护多套、一键复制复用、对应所选活动；
- **活动方案独立配置**：关联活动（界定参与对象）+ 活动周期 + 奖励折算标准（奖项等级→积分），**不再使用统一配置面板**。

---

## 2. 关键决策

| 决策点 | 结论 |
|--------|------|
| 奖励折算标准 | 奖项等级 → 积分折算表（如：一等奖 100 分） |
| 活动方案与活动关系 | 活动方案为独立模板，关联一个活动；参与对象由关联活动界定，**不单独配置粒度** |
| 活动方案积分构成 | **纯奖项折算**，活动方案不再保留四大维度计分面板 |
| 月度方案机制 | 固定存在、可编辑（权重/单条得分）、始终生效，不可复制/删除/停用 |
| 页面布局 | **页面分区**：月度固定卡 + 活动方案区 |

---

## 3. 数据模型改造

将现有单一 `scoreSchemes` 数组拆分为两个数据结构（原型数据文件 `medal-data.js`）。

### 3.1 月度常规勋章积分方案（固定唯一 · 始终生效）

```js
monthlyScheme: {
  id: 1,
  name: '月度常规勋章积分方案',
  updatedAt: '2026-08-01 更新',
  dimensions: [
    { key: 'usage', name: '平台使用', points: 5, weight: 30, enabled: true, headCoef: 1.0, assocCoef: 0.8 },
    { key: 'interaction', name: '家园互动', points: 8, weight: 35, enabled: true, headCoef: 1.0, assocCoef: 1.0 },
    { key: 'promotion', name: '外部推广', points: 15, weight: 20, enabled: true, headCoef: 1.0, assocCoef: 1.2 },
    { key: 'conversion', name: '会员转化', points: 20, weight: 15, enabled: true, headCoef: 1.0, assocCoef: 1.0 },
  ],
}
```

- 移除 `isActive` 字段（始终生效，无需启用开关）；
- 保留 `dimensions` 四维计分配置（单条得分 / 权重 / 计分开关 / 班主任、配班差异化系数）。

### 3.2 活动方案（可多套 · 可复制 · 对应关联活动）

```js
activitySchemes: [
  {
    id: 1,
    name: '课件大赛专项积分方案',
    activityId: 3,                // 关联活动（决定参与对象）
    cycleStart: '2026-08-01',     // 活动周期（选关联活动自动带出，可手动改）
    cycleEnd: '2026-08-31',
    awardRules: [                 // 奖励折算标准：奖项等级 → 积分
      { level: '一等奖', points: 100 },
      { level: '二等奖', points: 60 },
      { level: '参与奖', points: 20 },
    ],
    updatedAt: '2026-08-05 更新',
  },
]
```

- 活动方案**不设全局启用开关**，其状态由活动周期推导（未开始 / 进行中 / 已结束）；
- 参与对象不设独立字段，由 `activityId` 对应的活动界定（展示时提示该活动报名教师）。

### 3.3 数据种子与重置

- `MOCK` 中的 `scoreSchemes` 拆分后删除；
- `resetAll()` 中 `cache` 初始化 key 列表由 `'scoreSchemes'` 替换为 `'monthlyScheme'`、`'activitySchemes'`。

---

## 4. 页面布局

改造 `pc/admin.html` 的 `score-scheme` 内容块，由单一 `#schemeList` 容器改为两个分区。

```
┌─ 月度常规勋章积分方案 ──────────────────────────┐
│  标题 + 标签「固定唯一 · 始终生效」 + [编辑权重]   │
│  4 维度摘要行（沿用现有维度行样式）                │
└──────────────────────────────────────────────┘

┌─ 活动方案 ─────────────────────────────────────┐
│  [＋ 新增活动方案]                               │
│  方案卡片列表（activitySchemeList）：            │
│    ① 名称 + 状态标签（未开始 / 进行中 / 已结束）  │
│    ② 关联活动 · 参与对象（该活动报名教师，只读）   │
│    ③ 活动周期：2026-08-01 ~ 2026-08-31          │
│    ④ 折算标准：一等奖 100分 · 二等奖 60分 · …    │
│    [编辑] [复制方案] [删除]                      │
└──────────────────────────────────────────────┘
```

月度方案卡样式复用现有 `scheme-card` + `scheme-dim-row`。

---

## 5. 交互细节

| 操作 | 行为 |
|------|------|
| 月度「编辑权重」 | 打开现有 `weightDialog`（标题显示方案名），保存写入 `monthlyScheme.dimensions` |
| 「＋ 新增活动方案」 | 打开新弹窗 `activitySchemeDialog`（空表单） |
| 活动方案「编辑」 | 打开 `activitySchemeDialog` 并回填（名称 / 关联活动 / 周期 / 折算表） |
| 活动方案「复制方案」 | 生成副本：新 id、名称 + "（副本）"、**关联活动清空（需重新选择）**、活动周期与折算表保留；提示「已复制，请选择关联活动」 |
| 活动方案「删除」 | `confirm` 确认后删除 |
| 选择关联活动 | 自动带出该活动的活动周期（起止日期可改），并只读展示「参与对象：{活动名}报名教师」 |

### 5.1 新增/编辑活动方案弹窗（activitySchemeDialog）

```
方案名称*            [input]
关联活动*            [select：活动列表]  → 带出周期 + 只读参与对象提示
活动周期*            [date] ~ [date]
参与对象（只读）      「课件制作技能大赛 报名教师」
奖励折算标准*         [表格 CRUD：奖项等级 + 积分]
                      一等奖 [100]
                      二等奖 [60]
                      ＋ 添加奖项
```

- 折算表 CRUD 交互复用现有 `award-crud-table`（序号 / 奖项名称 / 数量）的增删行模式；
- 保存 / 取消按钮位于弹窗底部。

---

## 6. 校验与边界

- **活动方案保存**：
  - 方案名称必填；
  - 关联活动必填；
  - 折算表至少 1 行，且每行奖项等级 + 积分均必填（积分为非负整数）；
  - 不满足时 `toast` 提示并阻止保存。
- **活动方案状态标签**由周期推导：`cycleStart > 今天` 未开始；`cycleStart ≤ 今天 ≤ cycleEnd` 进行中；否则已结束。
- **月度方案**固定存在：页面不提供新增 / 复制 / 删除 / 停用入口。
- 方案快照与生效记录（`scoreLogs`）逻辑保持不变。

---

## 7. 改动文件清单

| 文件 | 改动内容 |
|------|----------|
| `medal-system/assets/js/medal-data.js` | `scoreSchemes` → `monthlyScheme` + `activitySchemes`；`resetAll()` key 列表更新；补充活动方案种子数据 |
| `medal-system/pc/admin.html` | `score-scheme` 内容块改为「月度卡 + 活动方案区」双容器；新增 `activitySchemeDialog` 弹窗 |
| `medal-system/assets/js/medal.js` | `renderScoreScheme` 重写为双区渲染；月度权重读写改读 `monthlyScheme`；删除 `score-enable` 动作；改造 `score-copy` 仅活动方案可用；新增活动方案新增/编辑/保存/复制/删除动作；`rerenderCurrentPage` 兼容新容器 |

---

## 8. 非本次范围

- 勋章门槛（`medalThresholds`）、奖金梯度（`bonusGradients`）配置不变；
- 移动端（`medal-system/mobile/`）不改动；
- `scoreLogs` 生效记录逻辑不改动；
- 本次仅改静态原型，不涉及后端与正式前端代码。

---

*文档版本: v1.0 | 创建日期: 2026-08-12*
